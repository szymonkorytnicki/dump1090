import { styled } from "../styled/Styled";
import * as style from "./ResponsiveWrapper.css";

export const MobileOnly = styled("div")(style.mobileOnly);
export const DesktopOnly = styled("div")(style.desktopOnly);
